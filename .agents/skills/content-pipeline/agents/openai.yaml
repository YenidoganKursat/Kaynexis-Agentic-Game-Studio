interface:
  display_name: "Content Pipeline"
  short_description: "Shape how assets, scripts, dialogue, or level content move from authoring into the game safely and repeatably. Use when content throughput or consistency is weak."
  default_prompt: "$content-pipeline "
policy:
  allow_implicit_invocation: true
