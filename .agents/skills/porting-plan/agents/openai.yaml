interface:
  display_name: "Porting Plan"
  short_description: "Map a PC/console/mobile/web port into concrete deltas for input, performance, compliance, assets, and release sequencing. Use before or during porting work."
  default_prompt: "$porting-plan "
policy:
  allow_implicit_invocation: true
