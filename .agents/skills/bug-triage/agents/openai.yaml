interface:
  display_name: "Bug Triage"
  short_description: "Convert a vague bug into reproducible steps, hypotheses, severity, owner, and next action. Use for crash, regression, UI, gameplay, or content bugs."
  default_prompt: "$bug-triage "
policy:
  allow_implicit_invocation: true
