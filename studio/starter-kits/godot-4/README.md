# Godot 4 Starter Kit

Use this kit when the repo should land on Godot 4.x with a lightweight scene-and-script baseline.

Key assumptions:

- `project.godot` exists at the repo root
- runtime logic lives under `src/`
- export settings stay under `export_presets.cfg`
- smoke/export validation uses the shared scripts in `scripts/`
