# Content

Put authored Unreal assets under `Content/`.

The starter kit keeps this folder in version control so routing, docs, and validation can point to a deterministic content root before the first binary asset lands.
