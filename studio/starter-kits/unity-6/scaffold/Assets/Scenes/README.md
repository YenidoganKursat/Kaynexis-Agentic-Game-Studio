# Scenes

Keep the first runtime scene under `Assets/Scenes/`.

The real `.unity` scene asset should be created in the editor, but the repository contract starts here so docs and pipelines can point to a deterministic location.
