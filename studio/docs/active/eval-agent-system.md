# Eval Plan — Agent System

## Change under test

- Add an umbrella agent-system guide and example that tie together the controller, role matrix, hierarchy, and prompt journal.

## Goal

- The repo should have one obvious entry point for multi-agent operating-model work.
- Single specialist mode should remain visible and available.
- The controller title should stay `Kaynexis` in user-facing summaries and route output.

## Validation path

1. Route a multi-agent-system request and confirm the agent-system docs appear first.
2. Confirm the route still shows single specialist mode as a valid path.
3. Confirm the prompt journal and hierarchy docs are surfaced for reviewable broad tasks.
4. Run the docs and test validators.

## Watch items

- Do not let the umbrella page replace the detailed portfolio or hierarchy docs.
- Do not let the controller title drift away from `Kaynexis`.
- Do not hide the single specialist mode behind the panel mode.
