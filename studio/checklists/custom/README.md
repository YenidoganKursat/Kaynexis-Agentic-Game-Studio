# Custom Checklist Overrides

Put project-specific checklist manifests here.

They are merged after base, engine, discipline, and milestone layers, so matching `item.id` values override the defaults.
