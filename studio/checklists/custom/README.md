# Custom Checklist Overrides

Put project-specific checklist manifests here.

They are merged after base, engine, discipline, and milestone layers, so matching `item.id` values override the defaults.

Use `studio/checklists/discipline/custom.toml` for the shared custom architecture and rule-pack contract, and use this folder for project-specific house rules or override manifests that should win later in the merge order.
